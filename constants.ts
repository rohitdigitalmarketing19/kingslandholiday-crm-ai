
import { Lead, Agent, LeadIntent } from './types';

export const INITIAL_AGENTS: Agent[] = [];

export const INITIAL_LEADS: Lead[] = [];


export const APP_CONFIG = {
  BRAND_NAME: 'Kingsland Holidays',
  MODEL: 'gemini-3-flash-preview',
  LUXURY_THRESHOLD: 80,
};
